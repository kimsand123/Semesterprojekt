version: '3'
services:
  gq-game-service:
    build:
      context: .
      dockerfile: docker/python/Dockerfile
    volumes:
      - ./:/GameService
    ports:
        - "9700:9700"
    command: python manage.py runserver 0.0.0.0:9700